import Typography from '@material-ui/core/Typography';
import React, { ReactElement } from 'react';

const Page404: React.FC = (): ReactElement => <Typography>404</Typography>;

export default Page404;
